export interface ScrollbarConfig {
  enabled: boolean
  width: string
  borderRadius: string
  borderWidth: string
  colors: {
    thumb: string
    thumbHover: string
    track: string
    border: string
  }
}
